<section class="row justify-content-center">
  <div class="col-4 row">
    <%= link_to("Create E-mail",emails_path, method: :post, class: "btn btn-primary my-3 col-4", remote: true) %>
    <h2 class="col-8 row my-3 text-primary justify-content-center">GibbZ Mail</h2>
  </div>
  <div class="col-6">
  </div>
</section>
<section class="row justify-content-center">
  <div class="col-4">
    <div class="list-group" id="toto">
    <% @emails.each do |my_mail| %>
      <%= render('email', var_mail: my_mail) %>
    <% end %>
    </div>
  </div>
  <div id="email_window" class="col-6">
    Aucun e-mail sélectionné pour le moment
  </div>
</section>